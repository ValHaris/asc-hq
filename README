FAQ :

THE COMPILE COMPLAINS CAN'T FIND SDL.

./configure --with-SDL-prefix=<where>
<where> should be the prefix you make installed SDL into.


Check out doc/readme.linux for information about running ASC.